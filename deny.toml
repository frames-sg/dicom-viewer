# Dependency policy for the full cross-platform desktop graph.

[graph]
all-features = false
no-default-features = false

[advisories]
version = 2
ignore = [
    # `encoding` is transitive through dicom-encoding 0.9.1. No safe upstream
    # upgrade exists. Review by 2026-10-01 or the next dicom-rs release.
    "RUSTSEC-2021-0153",
    # `paste` is transitive through metal 0.33. No safe upgrade exists.
    # Review by 2026-10-01 or the next metal-rs release.
    "RUSTSEC-2024-0436",
]

[licenses]
version = 2
allow = [
    "0BSD",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "BSL-1.0",
    "CC0-1.0",
    "ISC",
    "MIT",
    "OFL-1.1",
    "Ubuntu-font-1.0",
    "Unicode-3.0",
    "Zlib",
]
confidence-threshold = 0.93

[bans]
multiple-versions = "warn"
wildcards = "deny"
highlight = "all"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
